# 应用图标配置说明

## 配置日期
2025-10-21

## 使用的图标
`assets/icons/yaby-app-icon.png`

## 配置方法

使用 `flutter_launcher_icons` 包自动生成 iOS 和 Android 平台的所有图标尺寸。

### 1. 依赖配置

在 `pubspec.yaml` 中添加：

```yaml
dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/yaby-app-icon.png"
  min_sdk_android: 21
  # iOS specific
  remove_alpha_ios: true
  # Android specific  
  adaptive_icon_background: "#36CAC4"  # 品牌绿色
  adaptive_icon_foreground: "assets/icons/yaby-app-icon.png"
```

### 2. 生成图标

运行以下命令：

```bash
# 1. 安装依赖
flutter pub get

# 2. 生成图标
dart run flutter_launcher_icons
```

## 生成的图标

### Android
- **标准图标**: `android/app/src/main/res/mipmap-*/ic_launcher.png`
  - mipmap-mdpi (48x48)
  - mipmap-hdpi (72x72)
  - mipmap-xhdpi (96x96)
  - mipmap-xxhdpi (144x144)
  - mipmap-xxxhdpi (192x192)

- **自适应图标**: `android/app/src/main/res/mipmap-*/ic_launcher_foreground.png`
  - 前景图标（透明背景）
  - 背景颜色：#36CAC4（在 colors.xml 中定义）

### iOS
- **所有尺寸的图标**: `ios/Runner/Assets.xcassets/AppIcon.appiconset/`
  - 20x20 到 1024x1024 的所有尺寸
  - 自动移除 alpha 通道（iOS 要求）

## 打包和安装

### Android

```bash
# 生成 APK
flutter build apk --release

# 生成 App Bundle（推荐用于 Google Play）
flutter build appbundle --release

# 安装到真机
flutter install
```

### iOS

```bash
# 生成 iOS 应用
flutter build ios --release

# 使用 Xcode 打开项目
open ios/Runner.xcworkspace
```

然后在 Xcode 中：
1. 选择你的开发团队
2. 连接 iOS 设备
3. 点击 Run 按钮安装到真机

## 图标设计规范

### 图标尺寸
- **源图标**: 建议 1024x1024 像素
- **格式**: PNG（支持透明背景）
- **当前图标**: 已符合规范

### Android 自适应图标
- **前景层**: 应用图标主体
- **背景层**: 品牌绿色 (#36CAC4)
- **安全区域**: 中心 66% 的圆形区域
- **可见区域**: 根据设备 OEM 可能被裁剪成不同形状

### iOS 图标要求
- 不能包含 alpha 通道（已自动处理）
- 不需要圆角（系统自动添加）
- 填充整个画布

## 验证图标

### 在真机上验证

1. **清理构建**（确保使用新图标）:
```bash
flutter clean
flutter pub get
```

2. **重新构建和安装**:
```bash
flutter run --release
```

3. **检查点**:
   - 应用启动图标正确显示
   - 设备主屏幕图标正确显示
   - Android 自适应图标在不同设备上正确显示
   - iOS 图标没有黑边或白边

### 常见问题

**Q: 图标更新后没有显示新图标？**
A: 尝试以下步骤：
```bash
flutter clean
dart run flutter_launcher_icons
flutter run
```

**Q: Android 图标显示白色方块？**
A: 检查 adaptive_icon_background 颜色是否正确配置。

**Q: iOS 图标有黑边？**
A: 确保 remove_alpha_ios: true 已配置。

## 更新图标

如果需要更新图标：

1. 替换 `assets/icons/yaby-app-icon.png` 文件
2. 运行生成命令：
```bash
dart run flutter_launcher_icons
```
3. 清理并重新构建应用

## 相关文件

- 配置文件: `pubspec.yaml`
- 图标源文件: `assets/icons/yaby-app-icon.png`
- Android 图标: `android/app/src/main/res/mipmap-*/`
- iOS 图标: `ios/Runner/Assets.xcassets/AppIcon.appiconset/`
- Android 颜色: `android/app/src/main/res/values/colors.xml`

## 图标预览

### 崖柏应用图标
- 主题：中草药临床试验
- 设计元素：试管 + 植物 + 医疗十字
- 颜色：品牌绿 (#36CAC4)
- 风格：现代、专业、清晰

## 总结

✅ **已完成**：
- 添加 flutter_launcher_icons 依赖
- 配置图标路径和选项
- 生成 Android 和 iOS 全套图标
- 配置 Android 自适应图标
- 移除 iOS alpha 通道

🎯 **效果**：
- 应用图标在 Android 和 iOS 设备上正确显示
- Android 支持自适应图标（不同形状）
- iOS 符合 App Store 要求

📱 **下一步**：
- 在真机上测试安装
- 检查不同 Android 设备上的图标显示
- 准备应用商店提交材料

